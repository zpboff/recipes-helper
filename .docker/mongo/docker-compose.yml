version: "3.9"

services:
  mongo:
    image: mongo
    restart: always
    ports:
      - "27017:27017"
    env_file: .env      
    volumes:
      - data:/data/db
      - config:/data/configdb
volumes:
  data:
  config: